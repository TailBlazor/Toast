using Microsoft.AspNetCore.Components;

namespace TailBlazor.Toast
{
    public enum ToastPosition
    {
        TopLeft,
        TopRight,
        TopCenter,
        BottomLeft,
        BottomRight,
        BottomCenter
    }
}