using Microsoft.AspNetCore.Components;

namespace Tailwind.Toast
{
    public enum ToastPosition
    {
        TopLeft,
        TopRight,
        TopCenter,
        BottomLeft,
        BottomRight,
        BottomCenter
    }
}